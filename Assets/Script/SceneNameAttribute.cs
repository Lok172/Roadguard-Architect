using UnityEngine;

// This attribute marks a string field as holding a scene name, allowing
// a custom property drawer to render it as a scene picker in the Inspector.
public class SceneNameAttribute : PropertyAttribute { }